
import { Password } from './Password';
import styled from 'styled-components';

function App() {

  return (
    <AppWrapper>
        <Password/>
    </AppWrapper>
  );
}

const AppWrapper = styled.div`
  border-color: #fff;
`


export default App;
